import Image from "next/image";

type UserInfoProps = {
  profile: {
    profileImage: string;
    firstName: string;
  };
};
const UserInfo = () => {
  return <div>UserInfo</div>;
};

export default UserInfo;
